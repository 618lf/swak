<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	   xmlns:context="http://www.springframework.org/schema/context" 
	   xsi:schemaLocation="
	         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd"
	   default-lazy-init="true">

    <!-- 二级缓存 -->
	<bean id="ehCacheManager" class="org.springframework.cache.ehcache.EhCacheManagerFactoryBean">  
	    <property name="configLocation" value="classpath:redis/local-cache.xml"/>  
	</bean>
	<bean id="localCache" class="com.swak.common.cache.redis.RedisLocalCache">
	    <property name="cacheManager" ref="ehCacheManager"/>  
	</bean>
	
	<!-- 一级缓存 -->
	<context:property-placeholder ignore-unresolvable="true" location="classpath:redis/redis.properties"/>
	<bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
		<property name="maxTotal" value="${redis.pool.maxTotal}" />
		<property name="maxIdle" value="${redis.pool.maxIdle}" />
		<property name="maxWaitMillis" value="${redis.pool.maxWaitMillis}"/>
		<property name="testOnBorrow" value="${redis.pool.testOnBorrow}" />
	</bean>
	<bean id="jedisConnectionFactory" class="com.swak.common.cache.redis.factory.RedisConnectionFactory">
		<property name="hosts" value="${redis.hosts}"/>
		<property name="password" value="${redis.password}"/>
		<property name="timeout" value="${redis.readtimeout}"/>
		<property name="poolConfig" ref="jedisPoolConfig"/>
	</bean>
	<bean name="cacheManager" class="com.swak.common.cache.redis.RedisCacheManager"/>
	
	<!-- 解决两级缓存不一致的问题 -->
	<bean id="subscribeBooter" class="com.swak.common.cache.redis.RedisSubscribeBoot" scope="prototype"/>
</beans>